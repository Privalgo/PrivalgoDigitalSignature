# PrivalgoDigitalSignature
Sample project to hash, sign and encode a message into a DigitalSignature, then call an API that verifies that message against the DigitalSignature, outputting the result if successful.
